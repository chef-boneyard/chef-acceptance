driver:
  name: vagrant

provisioner:
  name: chef_zero
  product_name: chef
  product_version: latest
  channel: current

platforms:
  - name: centos-6.5
    run_list:

suites:
  - name: mixlib-install-chef
    run_list:
